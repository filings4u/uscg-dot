(()=>{'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=(v,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:String(c||'USD').toUpperCase()}).format(Number(v||0));
const fmt=v=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d)};
const pretty=v=>String(v??'—').replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase());
const badge=v=>`<span class="badge ${/active|paid|provisioned|succeeded/i.test(String(v))?'good':/cancel|failed|past_due|void/i.test(String(v))?'bad':'warn'}">${esc(pretty(v))}</span>`;
const planOf=o=>Array.isArray(o?.plans)?o.plans[0]:(o?.plans||{});
const subPlan=d=>Array.isArray(d?.subscription?.plans)?d.subscription.plans[0]:(d?.subscription?.plans||{});
const orgName=(d,ctx)=>d?.employer?.dba_name||d?.employer?.legal_name||d?.account?.legal_name||ctx?.membership?.organization_name||'Account';
const tracking=id=>`S4U-W-${String(id||'').replaceAll('-','').slice(0,10).toUpperCase().padEnd(10,'0')}`;
function orderRows(d){return (d.orders||[]).map(o=>{const p=planOf(o),amt=o?.metadata?.amount_total??p?.monthly_price??0,c=o?.metadata?.currency||'USD';return `<tr><td><strong>${esc(tracking(o.id))}</strong><small>${esc(o.reference||'')}</small></td><td>${esc(p.name||p.code||'DOT subscription')}</td><td>${fmt(o.paid_at)}</td><td>${money(amt,c)}</td><td>${badge(o.status)}</td><td><div class="billing-row-actions"><button class="mini-btn" data-receipt-kind="order" data-receipt-id="${esc(o.id)}">View Receipt</button><button class="mini-btn primary" data-download-kind="order" data-download-id="${esc(o.id)}">Download PDF</button></div></td></tr>`}).join('')}
function paymentRows(d){return (d.payments||[]).map(p=>`<tr><td><strong>${esc(p.external_payment_id||'Payment')}</strong><small>${esc(pretty(p.payment_method||'payment'))}</small></td><td>${fmt(p.paid_at||p.created_at)}</td><td>${money(p.amount,p.currency||'USD')}</td><td>${badge(p.status)}</td><td><div class="billing-row-actions"><button class="mini-btn" data-receipt-kind="payment" data-receipt-id="${esc(p.id)}">View Receipt</button><button class="mini-btn primary" data-download-kind="payment" data-download-id="${esc(p.id)}">Download PDF</button></div></td></tr>`).join('')}
function orderReceipt(o,d,ctx){const p=planOf(o),amt=o?.metadata?.amount_total??p?.monthly_price??0,c=o?.metadata?.currency||'USD';return {title:'Purchase Receipt',number:tracking(o.id),reference:o.reference||'',date:o.paid_at,description:p.name||p.code||'DOT subscription',amount:amt,currency:c,status:o.status,organization:orgName(d,ctx),detail:'Software subscription purchase'}}
function paymentReceipt(p,d,ctx){const sp=subPlan(d);return {title:'Payment Receipt',number:String(p.external_payment_id||p.id||'').slice(0,40),reference:d?.subscription?.order_reference||'',date:p.paid_at||p.created_at,description:sp.name||d?.subscription?.plan_name||'Subscription payment',amount:p.amount,currency:p.currency||'USD',status:p.status,organization:orgName(d,ctx),detail:pretty(p.payment_method||'subscription payment')}}
function getReceipt(kind,id,d,ctx){if(kind==='order'){const x=(d.orders||[]).find(z=>String(z.id)===String(id));return x?orderReceipt(x,d,ctx):null}const x=(d.payments||[]).find(z=>String(z.id)===String(id));return x?paymentReceipt(x,d,ctx):null}
function modalReceipt(r){if(!r)return;const bd=document.createElement('div');bd.className='modal-backdrop';bd.innerHTML=`<div class="modal invoice-preview-modal"><div class="invoice-modal-head"><div><h2>${esc(r.title)}</h2><p>${esc(r.number)}</p></div><button class="modal-x" type="button" data-close>×</button></div><div class="invoice-preview"><div class="invoice-preview-top"><img src="/assets/img/logo.png" alt="screenings4u DOT"><div><strong>RECEIPT</strong><span>${esc(r.number)}</span></div></div><div class="invoice-preview-meta"><div><small>FROM</small><strong>screenings4u DOT</strong><span>dot.screenings4u.com</span></div><div><small>ACCOUNT</small><strong>${esc(r.organization)}</strong><span>${esc(r.detail)}</span></div><div><small>PAID</small><strong>${fmt(r.date)}</strong><span>${esc(pretty(r.status))}</span></div></div><table class="invoice-preview-table"><thead><tr><th>Description</th><th>Reference</th><th>Amount</th></tr></thead><tbody><tr><td>${esc(r.description)}</td><td>${esc(r.reference||'—')}</td><td>${money(r.amount,r.currency)}</td></tr></tbody></table><div class="invoice-preview-totals"><div class="grand"><span>Total Paid</span><strong>${money(r.amount,r.currency)}</strong></div></div></div><div class="modal-actions"><button class="btn ghost" data-close>Close</button><button class="btn primary" data-pdf>Download PDF</button></div></div>`;document.body.appendChild(bd);bd.onclick=e=>{if(e.target===bd||e.target.closest('[data-close]'))bd.remove()};bd.querySelector('[data-pdf]').onclick=()=>downloadReceipt(r)}
async function imageData(url){try{const q=await fetch(url);if(!q.ok)return null;const b=await q.blob();return await new Promise(res=>{const fr=new FileReader();fr.onload=()=>res(fr.result);fr.onerror=()=>res(null);fr.readAsDataURL(b)})}catch{return null}}
async function downloadReceipt(r){if(!window.jspdf?.jsPDF){alert('PDF library is not available.');return}const {jsPDF}=window.jspdf,doc=new jsPDF({unit:'pt',format:'letter'}),logo=await imageData('/assets/img/logo.png');if(logo)try{doc.addImage(logo,'PNG',42,35,150,50,undefined,'FAST')}catch{}doc.setTextColor(16,47,85);doc.setFontSize(23);doc.text('RECEIPT',540,54,{align:'right'});doc.setFontSize(10);doc.setTextColor(90);doc.text(String(r.number||''),540,72,{align:'right'});doc.setTextColor(24,54,83);doc.setFontSize(12);doc.text('screenings4u DOT',42,120);doc.setFontSize(10);doc.setTextColor(90);doc.text('dot.screenings4u.com',42,136);doc.text(String(r.organization||''),330,120);doc.text(`Paid ${fmt(r.date)}`,330,136);doc.setFillColor(244,247,251);doc.rect(42,180,510,28,'F');doc.setTextColor(70);doc.text('DESCRIPTION',50,198);doc.text('REFERENCE',330,198);doc.text('AMOUNT',540,198,{align:'right'});doc.setTextColor(30);doc.text(String(r.description||''),50,230,{maxWidth:255});doc.text(String(r.reference||'—'),330,230,{maxWidth:130});doc.text(money(r.amount,r.currency),540,230,{align:'right'});doc.setDrawColor(220);doc.line(42,256,552,256);doc.setFont('helvetica','bold');doc.setFontSize(13);doc.text('TOTAL PAID',330,286);doc.text(money(r.amount,r.currency),540,286,{align:'right'});doc.setFont('helvetica','normal');doc.setFontSize(9);doc.setTextColor(105);doc.text('This receipt reflects the transaction recorded in your screenings4u DOT account.',42,742);doc.save(`screenings4u-dot-receipt-${String(r.number||'receipt').replace(/[^a-z0-9-]+/gi,'-')}.pdf`)}
window.AccountOrderHistory={render(d,ctx){const orders=d.orders||[],payments=d.payments||[],paid=payments.filter(x=>/succeeded|paid/i.test(String(x.status))).reduce((n,x)=>n+Number(x.amount||0),0);return `<div class="metrics"><div class="metric"><small>Purchases</small><strong>${orders.length}</strong><span>Subscription checkout orders</span></div><div class="metric"><small>Payments</small><strong>${payments.length}</strong><span>Recorded account payments</span></div><div class="metric"><small>Payment Total</small><strong>${money(paid)}</strong><span>Recorded successful payments</span></div><div class="metric"><small>Plan</small><strong>${esc(subPlan(d).name||d?.subscription?.plan_name||'—')}</strong><span>${esc(pretty(d?.subscription?.status||'unknown'))}</span></div></div><section class="panel section"><div class="panel-head"><div><h2>Purchase receipts</h2><p>View and download receipts for subscription purchases made through screenings4u DOT.</p></div></div><div class="table-wrap"><table><thead><tr><th>Receipt</th><th>Purchase</th><th>Paid</th><th>Amount</th><th>Status</th><th>Receipt</th></tr></thead><tbody>${orderRows(d)||'<tr><td colspan="6"><div class="empty">No subscription purchases are available yet.</div></td></tr>'}</tbody></table></div></section><section class="panel section"><div class="panel-head"><div><h2>Payment history</h2><p>Recurring subscription and account payments recorded for this account.</p></div></div><div class="table-wrap"><table><thead><tr><th>Payment</th><th>Paid</th><th>Amount</th><th>Status</th><th>Receipt</th></tr></thead><tbody>${paymentRows(d)||'<tr><td colspan="5"><div class="empty">No additional payments are available yet.</div></td></tr>'}</tbody></table></div></section>`},bind(d,ctx){$$('[data-receipt-kind]').forEach(b=>b.onclick=()=>modalReceipt(getReceipt(b.dataset.receiptKind,b.dataset.receiptId,d,ctx)));$$('[data-download-kind]').forEach(b=>b.onclick=()=>{const r=getReceipt(b.dataset.downloadKind,b.dataset.downloadId,d,ctx);if(r)downloadReceipt(r)})}};
window.AccountSubscription={render(d){const s=d.subscription||{},p=subPlan(d),ents=d.entitlements||{},items=d.subscription_items||[],enabled=Object.entries(ents).filter(([,v])=>v===true).map(([k])=>k);const price=p.monthly_price??items.find(x=>x.item_type==='base_saas')?.unit_amount;return `<div class="metrics"><div class="metric"><small>Current Plan</small><strong>${esc(p.name||s.plan_name||'—')}</strong><span>${esc(p.code||s.plan_code||'')}</span></div><div class="metric"><small>Status</small><strong>${esc(pretty(s.status||'—'))}</strong><span>${s.cancel_at_period_end?'Ends at current period':'Subscription access'}</span></div><div class="metric"><small>Billing</small><strong>${price!=null?money(price):'—'}</strong><span>${esc(pretty(s.billing_frequency||'monthly'))}</span></div><div class="metric"><small>Renews</small><strong>${fmt(s.renewal_date||s.stripe_current_period_end)}</strong><span>Current billing period</span></div></div><section class="panel section"><div class="panel-head"><div><h2>Subscription details</h2><p>Your screenings4u DOT software subscription and account limits.</p></div></div><div style="padding:20px"><div class="metrics">${s.start_date?`<div class="metric"><small>Started</small><strong>${fmt(s.start_date)}</strong></div>`:''}${s.employee_limit!=null?`<div class="metric"><small>Employee limit</small><strong>${esc(s.employee_limit)}</strong></div>`:''}${s.driver_limit!=null?`<div class="metric"><small>Driver limit</small><strong>${esc(s.driver_limit)}</strong></div>`:''}<div class="metric"><small>Order reference</small><strong>${esc(s.order_reference||'—')}</strong></div></div></div></section><section class="panel section"><div class="panel-head"><div><h2>Included software access</h2><p>Features enabled for the current subscription.</p></div><span class="badge good">${enabled.length} enabled</span></div><div style="padding:20px"><div class="cards">${enabled.map(x=>`<div class="card"><strong>${esc(pretty(x))}</strong><span>Included with your current plan.</span></div>`).join('')||'<div class="empty">No feature list is available for this subscription.</div>'}</div></div></section>`},bind(){}};
})();
